const express = require('express');const cors = require('cors');const bcrypt = require('bcryptjs');const fs = require('fs');const app = express();app.use(cors());app.use(express.json());const users = JSON.parse(fs.readFileSync('./users.json'));app.post('/api/admin/login', async (req, res) => {const { username, password } = req.body;if (!users[username]) return res.status(401).json({ message: 'Invalid user' });const match = await bcrypt.compare(password, users[username]);if (match) res.json({ message: 'Login successful', token: 'dummy-jwt-token' });else res.status(401).json({ message: 'Invalid password' });});app.listen(5000, () => console.log('Server running on port 5000'));