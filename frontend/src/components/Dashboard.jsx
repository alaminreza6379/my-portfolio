import { useState } from 'react';import AddProject from './AddProject';export default function Dashboard() {const [projects, setProjects] = useState([]);const addProject = (project) => {setProjects([...projects, project]);};return (<div style={{ padding: '20px', backgroundColor: '#1a1a1a', color: 'white', minHeight: '100vh' }}><h1>Admin Dashboard</h1><AddProject onAdd={addProject} /><h2>Projects</h2><ul>{projects.map((p, i) => <li key={i}>{p.title}</li>)}</ul></div>);};